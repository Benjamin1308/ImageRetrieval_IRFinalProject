GenerateDocument.py is for retrieve keyword from image dataset, output is document.json

GenerateDocVectorList is to generate index, vector for each keyword set of each image, output is is inverted_index.json, doc_vector_list.json

RetrieveImage take an base64 as query and output list of 10 most relevant images from dataset usign cosine relevance.
